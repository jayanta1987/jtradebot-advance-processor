spring:
  kafka:
    consumer:
      topic: jtradebot-kiteconnect-ticks
  data:
    mongodb:
      uri: mongodb+srv://${MONGODB_USERNAME:jtradebotuser}:${MONGODB_PASSWORD:ByQMNv50k6wWZDXj}@${MONGODB_CLUSTER:jtradebot-prod-new}.czqpoxe.mongodb.net/${MONGODB_DATABASE:jtradebot}?retryWrites=true&w=majority&appName=appName=jtradebot-prod-new
      database: ${MONGODB_DATABASE:jtradebot}
      auto-index-creation: true

logging:
  level:
    root: INFO
    org:
      mongodb:
        driver: INFO

# Balance Check Configuration
balance-check:
  scheduler:
    enabled: false  # Enable/disable automatic balance checking via scheduler
